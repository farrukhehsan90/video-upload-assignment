import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonContainer: {
        borderRadius: 10,
        padding: 20,
        width: '90%',
        marginTop: 20       
    },
    buttonText: {
        fontSize: 16,
        fontWeight: "bold",
        alignSelf: "center"
    }
});